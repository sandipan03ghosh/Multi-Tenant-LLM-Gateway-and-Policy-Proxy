{
  "name": "@llm-gateway/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Operator CLI — wraps the Admin and Public APIs via @llm-gateway/sdk-node. No direct DB/Redis access.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "llm-gateway": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@llm-gateway/sdk-node": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
