{
  "name": "@llm-gateway/adapters-redis",
  "version": "0.1.0",
  "private": true,
  "description": "Redis-backed implementations of domain ports: rate limiting, circuit breaker state, priority queues, config cache/pub-sub.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@llm-gateway/domain": "workspace:*",
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
