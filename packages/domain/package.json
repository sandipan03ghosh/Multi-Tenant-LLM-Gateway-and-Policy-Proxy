{
  "name": "@llm-gateway/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Core entities, value objects, domain services, and port interfaces. Zero infrastructure dependencies.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
