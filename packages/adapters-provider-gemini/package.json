{
  "name": "@llm-gateway/adapters-provider-gemini",
  "version": "0.1.0",
  "private": true,
  "description": "ProviderPort implementation for Google Gemini.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@llm-gateway/adapters-observability": "workspace:*",
    "@llm-gateway/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
