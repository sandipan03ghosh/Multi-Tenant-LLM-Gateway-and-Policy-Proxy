{
  "name": "@llm-gateway/adapters-postgres",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma-backed implementations of domain repository ports (tenancy, policy, audit, cost ledger).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@llm-gateway/adapters-observability": "workspace:*",
    "@llm-gateway/domain": "workspace:*",
    "@prisma/adapter-pg": "^7.9.0",
    "@prisma/client": "^7.9.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "prisma": "^7.9.0",
    "typescript": "^5.9.3"
  }
}
